# Cursor Active Users Bot - Local Development Environment Variables

# Service Configuration
# Enable/disable specific services (default: Cursor=true, GitHub=false)
ENABLE_CURSOR=true
ENABLE_GITHUB_COPILOT=false

# Cursor API credentials - get these from your Cursor admin settings
# Required when ENABLE_CURSOR=true
CURSOR_API_KEY=your_cursor_api_key_here

# GitHub Copilot configuration
# Required when ENABLE_GITHUB_COPILOT=true
GITHUB_TOKEN=your_github_token_here
GITHUB_ORG=your_github_organization_name

# Slack bot configuration - create a Slack app and get these tokens
# Your Slack app needs these permissions: chat:write, users:read.email
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token-here
SLACK_SIGNING_SECRET=your-slack-app-signing-secret-here
# Recipient of the notification
# Use channel_id or @user_id
SLACK_USER_ID=U1234567890

# Notification timing (in days)
NOTIFY_AFTER_DAYS=60
REMOVE_AFTER_DAYS=90

# AWS account details
AWS_ACCOUNT=
AWS_REGION=

# Port for the local Express server (optional, defaults to 3000)
PORT=3000

